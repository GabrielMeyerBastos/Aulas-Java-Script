// JOIN

const nomes = ["Gabriel", "Ana", "Lucas"];

console.log(nomes);

// join transforma o array em string
const listanomes = nomes.join(", ");

console.log(listanomes);

// INDEXOF

const posicaoitem = nomes.indexOf("Ana");

// mostra a posição do item dentro do array
console.log(posicaoitem);


